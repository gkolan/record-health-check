<?xml version='1.0' encoding='UTF-8' ?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>IsActive__c</fullName>
    <label>Active</label>
    <description
  >When checked, this check is included whenever its Check Set runs. When unchecked, the check is ignored during evaluation - a way to disable it temporarily without deleting the record.</description>
    <type>Checkbox</type>
    <defaultValue>true</defaultValue>
    <inlineHelpText
  >Checked = the check runs as part of its Check Set. Uncheck to disable it without deleting it.</inlineHelpText>
</CustomField>
